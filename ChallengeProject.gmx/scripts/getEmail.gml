#define getEmail
///getEmail(first name, last name, final string, email domain);

return argument0 + argument1 + argument2 + argument3;

#define validate
///validate(username, password);
var u = argument0;
var p = argument1;

if(u == "a.shen.pro2014@gmail.com" && p == "Theflikr123"){
    return true;
} else {
    return false;
}
